import { Typography } from '@mui/material';

export default function Home() {
  return (
    <div>
      <Typography variant="h4">Welcome to LearnSphere</Typography>
      <Typography variant="body1">
        This is the home page with a list of courses.
      </Typography>
    </div>
  );
}
